# @package _global_
# Mirrors config.presets.python.training.latent_transfuser:ltf_physical_ai_av_trainvaltest; every omitted key keeps its schema default.
defaults:
- /policy@policy_config: ltf_nuplan
- /offline_data_sources/ltf_nuplan@offline_data_sources.physical_ai_av_train: physical_ai_av_train
- /offline_data_sources/ltf_nuplan@offline_data_sources.physical_ai_av_val: physical_ai_av_val
- /offline_data_sources/ltf_nuplan@offline_data_sources.physical_ai_av_test: physical_ai_av_test
- _self_
lightning_trainer_config:
  max_epochs: 15
  gradient_clip_val: 1.0
  gradient_clip_algorithm: norm
