# @package _global_
# Mirrors config.presets.python.training.transfuser:tf_mixed_nuplan_physical_ai_av_trainval; every omitted key keeps its schema default.
defaults:
- /policy@policy_config: tf_nuplan
- /offline_data_sources/tf_nuplan@offline_data_sources.nuplan_train: nuplan_train
- /offline_data_sources/tf_nuplan@offline_data_sources.nuplan_val: nuplan_val
- /offline_data_sources/tf_nuplan@offline_data_sources.physical_ai_av_train: physical_ai_av_train
- /offline_data_sources/tf_nuplan@offline_data_sources.physical_ai_av_val: physical_ai_av_val
- _self_
lightning_trainer_config:
  max_epochs: 15
  gradient_clip_val: 1.0
  gradient_clip_algorithm: norm
