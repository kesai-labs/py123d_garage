# @package _global_
# Mirrors config.presets.python.training.transfuser:tf_carla_train; every omitted key keeps its schema default.
defaults:
- /policy@policy_config: tf_carla
- /offline_data_sources/tf_carla@offline_data_sources.carla: carla
- _self_
